---
interface Props {
	icon: string,
    text: string
}
const { icon, text } = Astro.props;

---

<div class="flex flex-row gap-2 items-center">
    <img class="h-5" src={icon} alt=""><span class="text-base text-text">{text}</span>
</div>